[backends.ir]
enabled = true

[target]
features = ["configured-feature"]
no-default-features = true

[extractor]
name = "custom-extractor"
dependency = { package = "haloumi-extractor-runner", path = "extractor" }
preludes = ["alpha", "beta"]

[extractor.dependencies]
support = { path = "support" }
